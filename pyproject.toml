# Black and Ruff configuration for consistent, linted code
[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
select = ["E", "F", "W", "I", "N", "UP", "B"] # Common quality and bugbear rules
ignore = []
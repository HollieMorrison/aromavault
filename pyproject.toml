[tool.black]
line-length = 100
target-version = ["py312"]

[tool.ruff]
line-length = 100
target-version = "py312"
# Enable common rules + import sorter
lint.select = ["E", "F", "W", "I", "UP"]
# Ignore long-line warnings (E501) – Black handles wrapping pragmatically
lint.ignore = ["E501"]
